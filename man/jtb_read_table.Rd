% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input.R
\name{jtb_read_table}
\alias{jtb_read_table}
\title{Read a table from a file or accept one already in memory}
\usage{
jtb_read_table(data, sheet = 1, ...)
}
\arguments{
\item{data}{One of:
\itemize{
\item a \code{data.frame} (including a tibble or \code{data.table}), returned as-is;
\item a matrix, converted column-wise to a \code{data.frame};
\item a length-one character path to a file. The extension decides the
reader: \code{.xlsx}/\code{.xls}/\code{.xlsm} use \code{readxl::read_excel()}, \code{.csv} uses
\code{\link[utils:read.csv]{utils::read.csv()}}, \code{.tsv}/\code{.txt} use \code{\link[utils:read.delim]{utils::read.delim()}}.
}}

\item{sheet}{Worksheet to read when \code{data} is an Excel path: a sheet name
or a 1-based position. Ignored for every other input.}

\item{...}{Further arguments passed to the underlying reader
(\code{readxl::read_excel()}, \code{\link[utils:read.csv]{utils::read.csv()}} or \code{\link[utils:read.delim]{utils::read.delim()}}).}
}
\value{
A \code{data.frame}.
}
\description{
The search functions call this on their \code{data} argument, so anything
documented here is also accepted by \code{\link[=jtb_search_keyword]{jtb_search_keyword()}} and
\code{\link[=jtb_search_semantic]{jtb_search_semantic()}}.
}
\examples{
# A data frame passes straight through.
jtb_read_table(head(site_reports, 3))

# A file is read according to its extension.
csv <- tempfile(fileext = ".csv")
utils::write.csv(site_reports, csv, row.names = FALSE)
str(jtb_read_table(csv))
unlink(csv)

}
