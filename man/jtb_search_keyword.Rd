% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_keyword.R
\name{jtb_search_keyword}
\alias{jtb_search_keyword}
\title{Find rows containing a keyword}
\usage{
jtb_search_keyword(
  data,
  keyword,
  columns = NULL,
  output_csv = NULL,
  ignore_case = TRUE,
  whole_word = FALSE,
  regex = FALSE,
  match = c("any", "all"),
  add_match_info = FALSE,
  sheet = 1,
  ...
)
}
\arguments{
\item{data}{An Excel path, a delimited-file path, or a data frame. See
\code{\link[=jtb_read_table]{jtb_read_table()}} for the full list of accepted inputs.}

\item{keyword}{The word or short phrase to look for. A character vector of
length greater than one is combined according to \code{match}.}

\item{columns}{Columns to search, given as names (\code{c("term", "comment")}),
as 1-based positions (\code{c(2, 5)}), or as a logical mask. \code{NULL}, the
default, searches every column.}

\item{output_csv}{Optional path. When given, the returned rows are also
written there with \code{\link[utils:write.csv]{utils::write.csv()}} (\code{row.names = FALSE}).}

\item{ignore_case}{Match regardless of case. \code{TRUE} by default.}

\item{whole_word}{Require the keyword to sit on word boundaries, so
\code{"ache"} no longer matches \code{"headache"}. \code{FALSE} by default.}

\item{regex}{Treat \code{keyword} as a regular expression instead of literal
text. \code{FALSE} by default; when \code{TRUE}, \code{whole_word} is ignored and the
pattern is used as written.}

\item{match}{With several keywords, \code{"any"} (the default) keeps rows
matching at least one, \code{"all"} keeps rows matching every one. Under
\code{"all"} the keywords may match in different columns of the same row.}

\item{add_match_info}{Append two reporting columns to the result:
\code{.matched_columns} (the matching column names, comma separated) and
\code{.matched_keywords}. \code{FALSE} by default.}

\item{sheet}{Worksheet to read when \code{data} is a path to an Excel workbook.}

\item{...}{Passed on to \code{\link[=jtb_read_table]{jtb_read_table()}}.}
}
\value{
A \code{data.frame} of matching rows, in their original order, with the
original row names so you can trace a row back to its position in the
input. Zero matches give a zero-row data frame with the same columns, not
an error. The result carries a \code{"jtb_search"} attribute recording the
keyword, the columns searched and the number of rows matched.
}
\description{
Returns the subset of rows whose selected columns contain \code{keyword}. The
search is literal by default -- \code{"c++"} finds \code{"c++"}, not a regex error.
Row order is preserved; nothing is scored or reordered. For ranking by
meaning rather than by literal text, see \code{\link[=jtb_search_semantic]{jtb_search_semantic()}}.
}
\examples{
# Every row mentioning "headache", anywhere in the table.
jtb_search_keyword(site_reports, "headache")

# Restrict to two columns, by name or by position.
jtb_search_keyword(site_reports, "headache", columns = c("term", "comment"))
jtb_search_keyword(site_reports, "headache", columns = c(5, 6))

# Whole words only: "ache" no longer matches "headache".
nrow(jtb_search_keyword(site_reports, "ache", columns = "comment"))
nrow(jtb_search_keyword(site_reports, "ache", columns = "comment",
                        whole_word = TRUE))

# Several keywords, and a CSV copy of the result.
out <- tempfile(fileext = ".csv")
hits <- jtb_search_keyword(site_reports, c("nausea", "dizzy"),
                           columns = "comment", output_csv = out)
nrow(hits)
unlink(out)

}
\seealso{
\code{\link[=jtb_search_semantic]{jtb_search_semantic()}} for similarity ranking.
}
