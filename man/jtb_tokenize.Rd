% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{jtb_tokenize}
\alias{jtb_tokenize}
\title{Split text into normalised search tokens}
\usage{
jtb_tokenize(x, stopwords = jtb_stopwords(), stem = TRUE, min_chars = 2L)
}
\arguments{
\item{x}{A character vector.}

\item{stopwords}{Words to drop. Defaults to \code{\link[=jtb_stopwords]{jtb_stopwords()}}; pass
\code{character()} to keep everything.}

\item{stem}{Reduce words to a common root, so \code{"reported"} and
\code{"reporting"} collapse together. Uses \code{SnowballC::wordStem()} when that
package is installed and a built-in suffix stripper otherwise.}

\item{min_chars}{Drop tokens shorter than this, after stemming.}
}
\value{
A list of character vectors, one per element of \code{x}.
}
\description{
The tokeniser the semantic search runs on every cell: lower-case, split on
anything that is not a letter or digit, drop stop words and very short
tokens, then stem. Exported so you can see exactly what the scoring works
from.
}
\examples{
jtb_tokenize("Subject reported a severe HEADACHE after dosing.")
jtb_tokenize("Subject reported a severe HEADACHE after dosing.", stem = FALSE)
}
